  function search(event) {
        event.preventDefault(); // prevent the default form submission behavior
    
     
    
        const query = event.target.search2.value;
    
     
    
        // perform a search query using the query value
        // display the results on the page
      }